
class HeartsBotBase {
  pass (middleware) {
  }

  expose (middleware) {
  }

  pick (middleware) {
  }

  onTurnEnd (middleware) {
  }

  onRoundEnd (middleware) {
  }

  onDealEnd (middleware) {
  }
}

module.exports = HeartsBotBase;
